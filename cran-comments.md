## Submission

This is the first CRAN submission of `bigbang`.

## Test environments

- Local Linux (Pop!_OS 22.04), R 4.6.1: `R CMD check --as-cran`, including the
  PDF manual: 0 errors, 0 warnings.
- win-builder (R-devel, 2026-07-22 r90289 ucrt, Windows Server 2022):
  Status: 1 NOTE (the standard new-submission NOTE).
- R-hub v2: linux, windows, and macos runners, all passing.
- GitHub Actions on every push: Ubuntu (release, devel, oldrel-1), Windows
  (release), and macOS (release), with the PDF manual enabled — all five jobs
  passing on the release commit.
- A metapackage generated by the submitted source was built and checked with
  `R CMD check --as-cran`, including its PDF manual: 0 errors, 0 warnings.

The only remaining NOTE is the standard new-submission NOTE. The word
'Tidyverse' in the Title is quoted as a software name, and the README no longer
links to files outside the tarball (both flagged by an earlier win-builder run
and fixed for this submission).

## Pre-release data-safety defect

During pre-CRAN testing we found that an unreleased predecessor generated startup
cleanup code that could recursively remove working-directory folders named after
component packages. No CRAN release contained this code. We removed startup
installation and every cwd-relative cleanup path, made installation explicit, and
added regression tests that recreate the former failure conditions only inside
disposable temporary trees. We also provide a read-only scanner for internally
generated historical artifacts so users can classify and replace them without
loading the package.

## Reverse dependencies

There are no reverse dependencies because this is a new submission.
