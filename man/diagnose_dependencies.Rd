% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{diagnose_dependencies}
\alias{diagnose_dependencies}
\title{Diagnose implicit dependencies of local packages}
\usage{
diagnose_dependencies(packages, pkg_dir, ext = ".tar.gz")
}
\arguments{
\item{packages}{Character vector. Names (with version) of the local
packages to examine, e.g. \code{"conexiones_0.8.3"}.}

\item{pkg_dir}{Character. Directory containing the local archive files
(\code{.tar.gz}, \code{.zip}, etc.).}

\item{ext}{Character. Archive extension. Defaults to \code{".tar.gz"}.}
}
\value{
A named list with one entry per local package, each a list with two
elements:
\describe{
\item{matrix_refs}{Character vector of references to 'Matrix', with file and line.}
\item{class_refs}{Character vector of references to 'class', with file and line.}
}
}
\description{
Scans local packages for references to the recommended packages 'Matrix' and
'class', which can cause \verb{R CMD check} failures when they are used implicitly
but not declared as dependencies.
}
\details{
Extracts and scans the R source of each package for patterns that suggest
implicit use of 'Matrix' or 'class'. Useful for debugging \verb{R CMD check} errors
such as "there is no package called 'Matrix'" even when the package does not
appear to use it directly.
}
\examples{
\dontrun{
res <- diagnose_dependencies(
  packages = c("conexiones_0.8.3", "utiles_1.4"),
  pkg_dir = "path/to/local/archives"
)
res[["conexiones_0.8.3"]]
lapply(res, function(x) x$matrix_refs)
}
}
